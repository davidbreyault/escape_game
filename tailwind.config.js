module.exports = {
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [
    
  ],
}

// plugin(function({ addUtilities }) {
//   const newUtilities = {
//     '.shadow-inner-custom' : {
//       'box-shadow': 'inset Opx 5px 3px rgba(0,0,0, 0.4)'
//     },
//   }
//   addUtilities(newUtilities);
// })